[pytest]
testpaths = tests
# La suite está 100% limpia a fecha de v1.0 (46 tests, cero warnings,
# verificado con -W error). Cualquier warning nuevo debe corregirse o
# justificarse aquí con un filtro explícito y un comentario.
filterwarnings =
    error
